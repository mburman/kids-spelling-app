// Main entry point for the spelling app

import { initApp } from './app';
import './styles.css';

// Initialize app when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
  initApp();
});
